"""Deprecated — removed from patches.txt.

Job Applicant.job_title already IS the Link -> Job Opening in this HRMS build
(label "Job Opening"), so the extra custom-field link + backfill turned out to
be unnecessary. The stray Custom Field it created on erp-hr.local was deleted
manually right after discovery.
"""
